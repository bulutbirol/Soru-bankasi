import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.bulutbirol.smmmsorubankasi',
  appName: 'SMMM Soru Bankası',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
}

export default config
